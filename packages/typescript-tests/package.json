{
  "name": "realm-typescript-tests",
  "version": "3.6.4",
  "description": "Integration testing use of the TypeScript types exported by the root package",
  "private": true,
  "scripts": {
    "test": "../../node_modules/.bin/tslint -p . && ../../node_modules/.bin/tsc -p ."
  },
  "dependencies": {
    "realm": "file:../..",
    "tslint": "^6.0.0",
    "typescript": "^3.8.2"
  }
}
